{
    smtp {
        auth: true
        start-tls: true
        host: "localhost"
        port: "1025"
        ssl {
            trust: "localhost"
        }
    }
    senders {
        info {
            address: "info@domace.hr"
            name: "Domace Info."
        }
        auth {
            address: "auth@domace.hr"
            name: "Domace Auth."
        }
        no-reply {
            address: "no-reply@domace.hr"
            name: "Domace No-Reply"
        }
    }
}
