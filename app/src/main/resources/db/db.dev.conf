{
    jdbcUrl: "jdbc:postgresql://localhost:5432/domace"
    maximumPoolSize: 10
    dataSource: {
        driverClass: "org.postgresql.Driver"
        driver: "postgresql"
        database: "domace"
    }
}
